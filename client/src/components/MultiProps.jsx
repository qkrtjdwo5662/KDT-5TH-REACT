import React from 'react'

export default function MultiProps(props) {
  return (
    <div>
      <h2>props.text : {props.text} </h2>
      <h2>props.href : {props.href} </h2>
      <h2>props.userID : {props.userID} </h2>
    </div>
  )
}
